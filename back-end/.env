# PostgreSQL 데이터베이스 설정
DB_USER=jeonghwan
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fastapi_new

# 전체 데이터베이스 URL (위의 개별 설정들을 조합한 것)
# 새로운 깔끔한 PostgreSQL 데이터베이스 사용
DATABASE_URL=postgresql://jeonghwan@localhost:5432/fastapi_new
# 테스트용 SQLite 데이터베이스를 사용하려면 다음 줄의 주석을 해제하고 위 줄은 주석 처리하세요:
# DATABASE_URL=sqlite:///./test.db
