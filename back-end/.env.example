# 환경변수 설정 파일
# 실제 운영환경에서는 이 파일을 .gitignore에 추가하고, 
# 각 환경별로 적절한 값을 설정해야 합니다.

# JWT 토큰 암호화를 위한 시크릿 키
# 실제 운영환경에서는 강력한 랜덤 문자열로 변경해야 합니다.
JWT_SECRET_KEY=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

# 구글 OAuth2 설정
# 구글 개발자 콘솔에서 OAuth2 클라이언트 ID를 발급받아 설정하세요.
# https://console.developers.google.com/
GOOGLE_CLIENT_ID=your-google-oauth2-client-id

# 데이터베이스 설정 (기존 설정 유지)
DATABASE_URL=postgresql://username:password@localhost/dbname

# AWS S3 설정 (기존 설정 유지)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
S3_BUCKET_NAME=your-s3-bucket-name

# 외부 분석 서버 설정 (기존 설정 유지)
ANALYSIS_SERVER_URL=http://your-analysis-server.com

# SQS 설정 (작업 큐용)
SQS_QUEUE_URL=https://sqs.ap-northeast-2.amazonaws.com/975049946580/audio-analysis-queue
USE_SQS_QUEUE=true  # SQS 사용 여부 (true/false)


